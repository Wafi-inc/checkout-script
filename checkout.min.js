"use strict";
(function (t) {
  console.log("connected to checkout script window", t);
  const e =
      "https://account-dev.wafi.cash,https://connect-dev.wafi.cash,https://sandbox-checkout.wafi.cash,https://sandbox-account.wafi.cash,http://localhost:3000,http://localhost:3001".split(
        ","
      ),
    c = {
      launch: (c, n) => {
        const { selector: s, success: o, cancel: a, error: i } = n,
          r = document.getElementById(s);
        r.setAttribute("src", c),
          t.addEventListener(
            "message",
            function (t) {
              if (!e.includes(t.origin)) return;
              const { event: c, data: n } = t.data;
              "success" === c && o && (o(n), r.parentElement.removeChild(r)),
                "error" === c && i && i(n),
                "cancel" === c && a && (a(n), r.parentElement.removeChild(r));
            },
            !1
          );
      },
    };
  t.WafiCheckout = c;
})(window);
